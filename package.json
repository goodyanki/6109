{
  "name": "macro-intent-agent-network",
  "version": "0.1.0",
  "description": "AI-Powered On-Chain Agent Protocol with Intent Infrastructure (MVP)",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:scripts": "vitest run",
    "test:all": "npm run test && npm run test:scripts",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.ts --network localhost",
    "agent": "ts-node scripts/agent.ts",
    "relayer": "ts-node scripts/relayer.ts",
    "seed": "ts-node scripts/seedIntents.ts",
    "baseline": "ts-node scripts/runBaseline.ts",
    "batching": "ts-node scripts/runBatching.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/better-sqlite3": "^7.6.0",
    "@types/express": "^4.17.0",
    "@types/node": "^20.0.0",
    "@types/supertest": "^6.0.0",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1",
    "better-sqlite3": "^11.0.0",
    "cors": "^2.8.5",
    "ethers": "^6.13.0",
    "express": "^4.18.0"
  }
}
